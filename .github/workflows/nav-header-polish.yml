name: Navigation and header polish

on:
  push:
    branches: [feature/quickjs-bindings]
    paths: [tools/apply_nav_header_polish.py]

permissions:
  contents: write

jobs:
  polish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Apply navigation and header polish
        run: python3 tools/apply_nav_header_polish.py
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build pkg-config libgl1-mesa-dev libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libwayland-dev libxkbcommon-dev libudev-dev
      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug
      - name: Build
        run: cmake --build build -j2
      - name: Commit validated UI polish
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/ui/PageManager.cpp src/ui/UI.cpp src/ui/I18n.cpp
          if git diff --cached --quiet; then
            echo "No product changes"
            exit 0
          fi
          git commit -m "style: tighten navigation and show active theme"
          git push
