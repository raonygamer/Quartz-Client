#pragma once
#include "quartz/client/native/NativeDisassembly.hpp"

namespace quartz::client::ui
{
    class PageManager;
    struct PageContext;
    void drawObjectExperiments(PageContext& context, PageManager& manager);
}
